<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <nav>
      <a href="/"><%= title %></a>
      <div class="links">
        <a href="/logout">Logout</a>
        <a href="/message/new">Create Message</a>
      </div>
    </nav>
    <main>
      <%- include('errors') %>
      <form action="/message/update/<%= id %>" method="POST">
        <label for="title">Title:</label>
        <input
          type="text"
          name="title"
          id="title"
          placeholder="Title"
          required
        />
        <label for="text">Message:</label>
        <textarea
          name="text"
          id="text"
          placeholder="Message"
          required
        ></textarea>
        <button type="submit">Update Message</button>
      </form>
    </main>
  </body>
</html>
